{
  "name": " ",
  "version": "0.0.1",
  "description": "",
  "keywords": "",
  "license": "",
  "author": "",
  "scripts": {
    "build": "parcel build index.html --public-url /firejoker --no-content-hash",
    "dev": "parcel index.html --open",
    "deploy": "rm -rf dist/ && npm run build && gh-pages -d dist",
    "start": "npm run build && npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4",
    "sass": "^1.26.11"
  },
  "dependencies": {
    "gh-pages": "^3.1.0",
    "main.css": "^2.1.0",
    "normalize.css": "^8.0.1"
  }
}
